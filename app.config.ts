export default defineAppConfig({
  ui: {
    // Override default container for easier styling managment
    container: {
      base: 'bg-neutral-200 dark:bg-neutral-950 mx-0',
      padding: '',
      constrained: ''
    }
  }
})
